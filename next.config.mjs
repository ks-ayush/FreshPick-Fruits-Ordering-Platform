// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.shopify.com',
      'images.unsplash.com',
      'anotherglobaleater.wordpress.com',
      'm.media-amazon.com'
    ],
  },
}

export default nextConfig;